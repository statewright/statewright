# Statewright

<center>
<b>State Machines for AI Agents</b>
<br><br>

**Reliability. Cost Savings. Auditability.**

Without bigger models.

Ben Cochran - May 2026
</center>

Note: Title card. Let it sit for a moment, then advance.

---

## About Me

- 20+ years full-stack engineering
- Indy Hackers Vice President
- Stints at NVIDIA, AMD
- Previously a Distinguished Engineer at a top 10 accounting & advisory firm
    - Technical architect for ~50 engineers and data scientists
- Now: Serial Solo Founder/CTO, open source builder

Note: Quick background. The DE role is where I saw compliance theatre up close — policies nobody enforced, agents with carte blanche. That experience led directly to what I'm showing you today.

---

## Open Source Adventures

**pg-pocketbase** — PocketBase on PostgreSQL. Because SQLite doesn't cut it in production. 13 releases this month.

**Trove** — lightweight S3-compatible object store in Go. Single binary, PostgreSQL metadata, 28MB Docker image.

**Statewright** — state machine enforcement for AI agents. 360 stars on GitHub.  HN Front Page post. What we'll talk about today.

Note: I build things that scratch my own itch. pg-pocketbase because I needed Postgres in production. Trove because I needed cheap self-hosted object storage. Statewright because I watched agents destroy things and decided to fix the harness instead of blaming the model.

---

## The Problems Everyone Has

You gave your agent access to everything.

It went in circles.  A Death Spiral.

It contradicted itself.  It confidently made the wrong fix.

Worse: It whipped up a nasty bash command that deleted your data.

Note: Show of hands — who has watched an agent read the same file 3 times, list the directory, write a reproduce script, run it, and then read the file again? That's the dominant failure mode. 8 of 10 unconstrained runs in our experiments did exactly this.

---

## Dollars All The Way Down

> "Just use a bigger model."

- ~$0.05-0.10/step × 20 steps ≈ **$1-2 per workflow**
- 100 runs/day = **$3,000-6,000/month**
- And it still sometimes spirals

Note: The industry response is always "more parameters, more context, more money." But the model isn't the bottleneck. The harness is.

---

## What's Happening in the Market

Four+ projects converged this month alone:

- **Forge** (TI) — retry nudges, parse rescue
- **Wardwright** — model routing proxy
- **HALO** — harness scheduling
- **Statewright** — state machine enforcement

Same thesis: **the harness is first-class infrastructure.**

Note: When three independent builders converge on the same problem in the same month, it's a market, not a coincidence. Forge focuses on tool-call retry. Wardwright on model routing. Statewright on workflow-level enforcement. Different layers of the same stack.

---

## The Thesis

> **Agents are suggestions. States are laws.**

The model can rationalize away instructions.

It can't rationalize away a blocked tool call.

Note: This is the one-liner. A state machine doesn't negotiate with the model. It doesn't care how confident the model is. If Edit isn't in the allowed tools for this phase, the call gets rejected. Period.

---

## What a Workflow Looks Like

```json
{
  "planning": {
    "allowed_tools": ["Read", "Grep"],
    "max_iterations": 5
  },
  "implementing": {
    "allowed_tools": ["Read", "Edit"],
    "max_iterations": 8
  },
  "testing": {
    "allowed_tools": ["Read", "Bash"],
    "allowed_commands": ["*test*"]
  }
}
```

Note: Three phases. Planning is read-only — the model literally cannot edit. Implementation unlocks Edit. Testing restricts Bash to test commands only. Each phase has an iteration budget. This is the entire enforcement layer — it's a data structure, not a prompt.

---

## When the Model Disobeys

```
⏺ Edit(src/main.py)
  ⎿ PreToolUse hook (blocked)

  Tool 'Edit' not available in 'planning'.
  Allowed: Read, Grep, Glob.
  Transition with: READY → implementing.
```

The model sees this. It adjusts.

Note: The hook blocked it, told it exactly what IS available, and how to advance. The model doesn't spiral — it gets concrete feedback. This is enforcement, not a suggestion.

---

## The Data

| Metric | Before | After |
|--------|--------|-------|
| SWE-bench (20-27B models) | 20% | **100%** |
| API calls (Haiku) | 29 | **18** |
| Min viable model | 20B+ | **sub-3B** |

Same task. Same model. Different structure.

Note: Three independent findings. 20% to 100% is across 5 SWE-bench tasks, 2 models (gemma4:31b ~27B, gpt-oss ~21B), 10 total runs. 38% fewer API calls from a mock infrastructure task. The sub-3B result is gemma4:e2b — 2.3B effective parameters, 7.2GB on disk. The tool interface change (content-matching edit) dropped the floor by an order of magnitude.

---

## Why Smaller Models Work

The model doesn't get 30 tool definitions.

It gets 3.

```
Planning:  3 tools   (~1,000 tokens)
Full set:  30 tools  (~12,000+ tokens)
```

On a model with 16K context, 30 tools eats **most** of it.

Per-state scoping: **nearly 3x more room for code.**

Note: Tool definitions average 200-600 tokens each depending on schema complexity. MCP tools with nested params are on the high end. The exact overhead depends on your tool set, but the ratio is consistent — fewer tools in scope means dramatically more context for actual code and reasoning.

---

## The Sub-3B Self-Correction

gemma4:e2b — 2.3B effective parameters, 7.2GB on disk.

Tried a complex tool 3 times. Failed each time.

Auto-test caught it. Bounced back to implementing.

**4th cycle: switched to a simpler tool.**

One call. One line. All tests green.

Note: The model couldn't serialize JSON for patch_file — capability limitation at this size. But the retry loop (implementing → testing → implementing) gave it enough attempts to discover edit_line, a simpler tool that only needs old text and new text. The guardrails provided the safety net. The model provided the adaptation. A 2.3B model doing what previously needed 20B+.

---

## Per-State Model Routing

Not every phase needs a frontier model.

```
planning:      gemma4:12b  (local)
implementing:  llama3.3    (local)
testing:       gemma4:12b  (local)
critical call: Opus        ($0.10)
```

**Significant cost reduction. Same results.**

Note: Pi's extension API supports setModel(). Claude Code can't do this. Codex can't do this. The cheap model handles 18 of 20 steps. The frontier model fires once for the one decision that matters.

---

## Auditability

Every transition is logged with rationale.

```
planning → implementing
  "Bug in converter.py:11"
implementing → testing
  "Fixed formula: 9/5 → 5/9"
testing → implementing
  "test_roundtrip still failing"
testing → completed
  "All 6 tests green"
```

Not observability. A **decision trace.**

Note: If someone asks "why did the agent change this file?" — the answer is in the transition log. Each transition carries a rationale. Each phase has constrained tools. You can reconstruct what the agent could and couldn't do at every point.

---

## Self-Hosted Stack

```
Pi (any model, any provider)
  └── Statewright extension
        ├── Gateway (Rust, MCP)
        │     └── SQLite (PocketBase)
        └── Ollama (local inference)
```

No cloud. No API keys. Engine is Apache 2.0.

Note: Pi and Ollama are MIT, Statewright engine & supporting self-hosted code are Apache 2.0. Gateway/Plugins are FSL with 3-year conversion. Self-host everything on a single machine with commodity hardware.

---

## Demo

1. Pi + Statewright + local Ollama
2. Load the bugfix workflow
3. Tool enforcement in real-time
4. Fork/join — parallel sub-agents
5. Transition audit trail

Note: Everything running locally. No cloud. The workflow is the standard bugfix template — planning, implementing, testing.

--

## Demo: Install

```bash
git clone github.com/statewright/statewright
cd statewright/self-hosted
docker compose up --build
# API key + alias printed to logs
```

```bash
# From the fixture directory:
cd self-hosted/fixtures/fork-test
lspi --model gemma4:31b
```

Note: Docker compose brings up PocketBase (UI on :8090) + Gateway (:3001). Migration seeds superuser, local user, API key, and bugfix workflow. The lspi alias reads the API key from the container automatically. Fixture: 3 buggy source files, 13 tests total, 8 failing.

--

## Demo: Planning Phase

```
> load bugfix workflow, fix failing tests

[sw] Phase: planning. Tools: read, grep.

⏺ grep("fahrenheit", ".")
⏺ read(src/converter.py)
⏺ read(src/formatter.py)
⏺ read(src/weather.py)

[sw] Transition: READY → implementing
```

Read-only. The model can't edit yet.

Note: Three source files, three bugs. Planning forces the model to read everything before touching anything. No premature editing.

--

## Demo: Implement + Test

```
[sw] Phase: implementing. Tools: read, edit.

⏺ edit(src/converter.py)
  (f - 32) * 9/5 → (f - 32) * 5/9
⏺ edit(src/formatter.py)
  "C" → "°C", "F" → "°F"
⏺ edit(src/weather.py)
  fix display format in daily_summary

[sw] Transition: DONE → testing

⏺ bash(pytest tests/ -v)
  13 passed ✓

[sw] Transition: TESTS_PASS → completed
```

Note: Implementation unlocks edit. Targeted fixes across multiple files. Testing runs pytest — restricted to test commands via allowed_commands.

--

## Demo: Fork/Join

```
> statewright_fork(branches: [
  {branch:"converter", task:"fix converter"},
  {branch:"formatter", task:"fix formatter"},
  {branch:"weather",   task:"fix weather"}
])

[sw] Fork: 3 branches dispatched
  [converter]  ✓ (4 turns, $0.00)
  [formatter]  ✓ (3 turns, $0.00)
  [weather]    ✓ (5 turns, $0.00)

[sw] Join: 3/3 succeeded
```

Note: Three parallel pi processes, one per bug. Each in its own branch session. Each constrained by the bugfix workflow. Each using a local model. All three bugs fixed independently, results collected.

---

## What You Can Take Home

1. **Per-state tool restriction**
2. **Iteration budgets**
3. **Programmatic states** for mechanical work
4. **Transition guards** in code, not prompts
5. **The tool interface matters**

Note: These patterns work regardless of framework. You can implement this in Claude Code hooks, a Python wrapper, a bash script. The point: structural enforcement beats prompt engineering.

---

## The Numbers

| What | Result |
|------|--------|
| SWE-bench (20-27B) | 20% → 100% |
| API calls (Haiku) | -38% |
| Min viable model params | 20B+ → sub-3B |
| Usable context (16K) | nearly 3x |

Note: Real numbers from real experiments. Research at statewright.ai/research. Engine is open source. Reproduce it.

---

# Questions?

---

## Links

<table style="width:100%; text-align:center; font-size:0.6em;">
<tr>
<td><img src="qr/linkedin.png" height="140"><br><b>LinkedIn</b><br>cochranb</td>
<td><img src="qr/research.png" height="140"><br><b>Research</b><br>statewright.ai</td>
<td><img src="qr/github.png" height="140"><br><b>GitHub</b><br>statewright</td>
</tr>
<tr>
<td><img src="qr/docs.png" height="140"><br><b>Docs</b><br>docs.statewright.ai</td>
<td><img src="qr/indyhackers.png" height="140"><br><b>IndyHackers</b><br>indyhackers.org</td>
<td><img src="qr/givebutter.png" height="140"><br><b>IH Race to 10k</b><br>givebutter.com</td>
</tr>
</table>
<center><small>GitHub stars and other means of support are appreciated.</small></center>

Note: Thank you. Happy to answer questions about the architecture, the experiments, or how to apply these patterns in your own workflows.
