# statewright-demo

Demo binary: an LLM creates its own state machine guardrails and then uses them to fix a bug. Requires an Ollama instance with a code model.

```bash
cargo run --bin sw-demo -- --model qwen2.5-coder:32b
```

Apache 2.0.

[docs.statewright.ai](https://docs.statewright.ai) | [GitHub](https://github.com/statewright/statewright)
